{% extends 'base.html.twig' %}

{% block title %}Erreur de paiement - SurfShop 40/64 {% endblock %}

{% block content %}
    <h2>Erreur de paiement</h2><br>
    <p>
        Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong>,<br>
        Il semblerait que votre paiement est échoué pour votre commande n°<strong>{{ order.reference }}</strong><br>
        Une confirmation d'erreur vient de vous être envoyée par email à l'adresse <strong>{{ order.user.email }}</strong>.
        <hr>
     </p>
    <a href="{{ path('order') }}" class="btn btn-primary">Réessayer le paiement</a>
{% endblock %}
